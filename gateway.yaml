---
apiVersion: networking.istio.io/v1
kind: GateWay
metadata: 
 name: gate
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
